services:
  sso-auth:
    image: ghcr.io/farismnrr/agentic-ai-code-sso-auth:refactor-full-fe-be-relay
    restart: unless-stopped
    env_file:
      - ./sso-auth/.env
    environment:
      PORT: "3000"
    ports:
      - "${SSO_PORT:-3000}:3000"
