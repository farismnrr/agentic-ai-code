# Relay HTTP service
PORT=3100
RELAY_PUBLIC_URL=http://localhost:3100

# SSO connection handoff
SSO_BASE_URL=https://sso.farismnrr.com
RELAY_ASSERTION_SECRET=
CONNECTION_ATTEMPT_TTL_SECONDS=300

# Optional. Must match the Client ID / audience registered in the SSO dashboard.
# RELAY_CLIENT_ID=relay-agent
