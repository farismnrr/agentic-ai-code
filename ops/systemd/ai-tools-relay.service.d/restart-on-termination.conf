# The relay handles SIGTERM with a graceful, successful exit.  Restarting
# only on failure therefore leaves the public MCP edge without an upstream
# after an externally signaled process exit.  Explicit systemctl stop still
# stops the unit; this covers an unexpected/direct process termination.
[Service]
Restart=always
